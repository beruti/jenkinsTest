The following project provides backend services to support demos and examples.

To start backend services run:
```
./gradlew bootRun
```
or if Maven is your build tool of choice:
```
mvn spring-boot:run
```

